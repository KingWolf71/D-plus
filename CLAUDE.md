don't use command line compiler as it won't work; I can compile manually via IDE
always leave definitions at beginning of procedure - no exceptions
No procedure static variables; we use global variables which can be reset between runs
if there is an _*.ver file add a .1 everytime we interact
